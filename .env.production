NEXT_PUBLIC_GOOGLE_MAP_KEY=
NEXT_PUBLIC_SITE_VERSION=3.6
# BACKEND_URL is set as a runtime env var on the Cloud Run service instead
# (see DEPLOY_GCP.md) so it can change without rebuilding the image.
